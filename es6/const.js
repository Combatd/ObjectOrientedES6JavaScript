"use strict"; // if you want errors to be enforced, no silent failures

const pi = 3.14;
// pi = 3.79; // throws an error
console.log(pi);

const product = {
  
};
// const product = Object.freese({}); // freeze the object
// you can change attributes inside the object / hashmap
product['name'] = 'Iphone';

console.log(product);